import { MaintenancePackages } from './MaintenancePackages'

/** Full maintenance package section for /service-onderhoud. */
export function MaintenancePlans() {
  return <MaintenancePackages variant="full" />
}
